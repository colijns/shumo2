# 无人机协同巡检（赛题 A）

天津市五校数学建模联赛 A 题：同一基地统一调度多架同型号无人机，在 9 h 内完成全部巡检任务并返回基地。本术语表统一论文、代码与结果文件的语言。

## 赛题实体

**巡检点（Inspection Point）**：
附件数据中的一个设施位置，具有空间坐标与巡检等级（I/II/III）。
_Avoid_: 设施、站点、客户点

**任务（Task）**：
一次"到达某巡检点并执行 5 min 作业"的独立事件；由巡检点按等级展开而来，同一点的多个任务互不相同。
_Avoid_: 节点、订单

**任务展开**：
I/II/III 级巡检点分别生成 3/2/1 个任务；原地延长停留不产生新任务。
_Avoid_: 复制、放大

**基地（Depot）**：
坐标 (0,0) 的唯一起降点；所有无人机从基地同时出发，完成自身全部任务后返回。
_Avoid_: 仓库、原点

**重复巡检**：
同一巡检点的多个任务被完成的情形。不同无人机分别到达可各自计数；同一无人机必须先离开再返回，相邻两次到达才算两次巡检。

## 时间与度量

**工作时间 S_k**：
第 k 架无人机飞行时间与巡检服务时间之和；飞行时间逐段向上取整到秒（保守口径），服务时间每任务 300 s。
_Avoid_: 航时、耗时

**完成时间 Tmax / Tmin**：
全部无人机中最长 / 最短的工作时间；Tmax 即总体完成时间。

**坐标换算**：
坐标 1 单位 = 0.1 km；两点间按欧氏直线飞行，速度 55 km/h。

## 优化模型

**min-max mTSP**：
带单机 9 h 时限的多旅行商问题；问题 1 的模型族——先求最少无人机数，再压缩最长路线时间。
_Avoid_: VRP、路径规划（泛称）

**词典序优化**：
"无人机数量"严格优先于"完成时间"的优化次序；总飞行距离仅作同 Tmax 下的次要判据。

**N_min 与 N_LB**：
N_min 为完成全部任务所需的最少无人机数；N_LB 为其可证下界（任务总服务时间 + MST 路程下界折算）。仅当下界处找到可行解时 N_min = N_LB 成立。

**紧档（Tight Case）**：
无人机数取下界 N_LB 但尚未证可行的算例——Case1 的 3 架、Case3 的 4 架。紧档求解是问题 1 优化的核心目标。

**独立校验（Independent Verification）**：
不执行求解、只对解档案纯重放的复算路径；逐条检查任务覆盖、路线闭合、同点禁排、时间口径与结果文件一致性。
_Avoid_: 复现、抽查

**PROVEN_BY_LOWER_BOUND / BEST_FEASIBLE_NOT_PROVEN**：
最优性标记：前者表示可行解取在下界处、无人机数严格最少；后者表示只在更大 N 处找到可行解、最小性未经证明。

**解档案（Solution Archive）**：
每算例落盘的完整调度记录（含任务序列与参数快照），是论文数字的唯一权威来源，可脱离求解器独立复算。
_Avoid_: 日志（泛指运行日志）
