# 问题3：冲突聚焦邻域完整运行报告

> 运行分支：`q3-conflict-focus-run`。候选结果与正式严格档案隔离保存，未自动晋级。

## 1. 运行方案

四个算例均从仓库当前问题3严格档案出发，采用固定随机种子 42。每轮优先检查瓶颈路线中等待、绕行或接近禁飞区启停边界的航段，聚焦航段数取 12；聚焦阶段停滞后允许回到原完整邻域。候选路线仍由原时变禁飞区评价器完整重算，不放松任何约束。

单算例最大搜索预算为 1800 秒。Case1—Case4 相互独立，并行运行；提前满足连续三轮全邻域无改进条件的算例允许提前结束。每个候选先在运行进程内校验，随后再统一独立重放校验。

## 2. 完整运行结果

| 算例 | 结果版本 | 最大完工时间/s | 最小完工时间/s | 极差/s | 完工时间和/s | 总等待/s | 总距离/km |
|---|---|---:|---:|---:|---:|---:|---:|
| Case1 | 原严格解 | 31255 | 31153 | 102 | 124768 | 0 | 1575.654316 |
| Case1 | 完整运行候选 | 31255 | 31153 | 102 | 124768 | 0 | 1575.654316 |
| Case2 | 原严格解 | 42719 | 42708 | 11 | 85427 | 4 | 666.771177 |
| Case2 | 完整运行候选 | **42004** | **42004** | **0** | **84008** | 85 | **643.850132** |
| Case3 | 原严格解 | 36767 | 36661 | 106 | 183537 | 1342 | 2140.739607 |
| Case3 | 完整运行候选 | **33903** | 33763 | 140 | **169166** | **0** | **1941.695844** |
| Case4 | 原严格解 | 35899 | 35850 | 49 | 143472 | 195 | 1353.346706 |
| Case4 | 完整运行候选 | 35899 | 35850 | 49 | 143472 | 195 | 1353.346706 |

实际搜索时间为：Case1 约 27.2 秒、Case2 约 1800.0 秒、Case3 约 1800.0 秒、Case4 约 1335.6 秒。Case1 与 Case4 因邻域停滞提前结束并保留原严格解；Case2 与 Case3 用满预算并取得严格改善。

## 3. 改善分析

Case2 的最大完工时间减少 715 秒，约下降 1.67%；两架无人机均在 42004 秒完成，极差由 11 秒降为 0。完工时间和减少 1419 秒，总距离减少约 22.92 km。总等待由 4 秒增加到 85 秒，但在最大完工时间、极差和完工时间和均改善的情况下，不影响其按当前字典序优于原解的结论。

Case3 的最大完工时间减少 2864 秒，约下降 7.79%；完工时间和减少 14371 秒，总等待由 1342 秒降为 0，总距离减少约 199.04 km。极差由 106 秒增加到 140 秒，仅增加 34 秒；由于第一层最大完工时间已经显著下降，候选仍是当前字典序下的明显改进。

Case1、Case4 没有找到更优路线，说明当前严格解在本次邻域和预算下较稳定，但不能据此宣称全局最优。

## 4. 校验与交付

四个候选档案均完成两层校验：运行结束校验与统一独立重放校验，全部违规项为空。生成的隔离交付包包括：

- 四个完整任务路线档案；
- 四份逐航段时间表；
- 含 Case1—Case4 四个工作表的结果工作簿；
- 汇总指标、候选来源哈希与验证状态。

交付目录：`outputs/workbooks/q3/conflict_focus_full_20260818_160134/`。

## 5. 建议

建议将 Case2、Case3 候选提交组内复核，并在确认“最大完工时间优先”的字典序口径后再晋级正式结果。Case1、Case4继续沿用原严格档案。正式晋级时应同时替换路线档案、时间表、结果工作簿和验证报告，避免不同文件之间数据不一致。
