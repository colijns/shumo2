# 问题4报告（统一口径修订版）

> 成文日期：2026-08-09

## 一、问题分析

本问研究两类导电介质混合填充时的最低成本问题。微构体为边长
\(L=10000\,\mathrm{nm}=10\,\mu\mathrm m\) 的立方体，仅垂直于 \(X\) 轴的左右两个表面带电。介质 A 为长度 5000 nm、底面半径 30 nm 的有限平端圆柱；介质 B 为半径 200 nm 的球体。介质 A、B 的单位体积成本分别为

\[
1.05\ \mathrm{元}/\mu\mathrm m^3,
\qquad
0.05\ \mathrm{元}/\mu\mathrm m^3.
\]

需要确定两类完整介质的填充数量 \((N_A,N_B)\)，使微构体导通概率不低于 90%，同时总成本最低。

与问题 3 的单变量临界填充量不同，本问是带概率约束的二维整数优化问题。直接对所有 \((N_A,N_B)\) 进行大样本蒙特卡洛计算代价过高，因此采用共同随机数、成本边界搜索和分级统计验证：先以小样本定位低成本候选区域，再对候选及其低成本邻点追加样本，最后使用未参与搜索的独立随机种子复算。

## 二、模型假设

1. 微构体区域为 \(\Omega=[-5000,5000]^3\)，左、右带电面分别为
   \(\Gamma_L:x=-5000\) 和 \(\Gamma_R:x=5000\)。
2. 介质 A 为有限平端圆柱，介质 B 为球体；两者的中心在微构体内均匀分布，介质 A 的方向服从三维各向同性分布。
3. 两类介质的位置相互独立，位置生成后不再移动；不考虑重力和碰撞，允许介质相互贯穿或重叠。
4. 两介质之间或介质与带电面之间的最短表面距离不超过
   \(\delta=1.8\,\mathrm{nm}\) 时视为电接触。
5. 介质越界部分按照题目给定的周期边界规则平移至相对侧；多方向同时越界时枚举组合平移。
6. 同一完整介质形成的多个周期片段共享来源编号，但不因同源关系自动建立电连接，各片段只按平移后的实际几何位置参与接触判定。
7. 填充量和成本均按边界处理前的完整介质计算，边界平移不改变介质数量及计量体积。
8. 候选方案使用共同随机序列的前缀，增加任一种介质只会增加节点和接触边，不会破坏已有导电通路。
9. 搜索阶段允许以点估计 \(\widehat P\ge0.90\) 定位候选；正式可靠方案必须满足 95% Wilson 区间下界不低于 0.90。

### 2.1 当前几何实现口径

本次正式运行采用几何修正版，但尚未构造真实周期片段的严格实体上下界：

- 介质 A 先按轴线与盒边界的交点分段、周期平移，再把各轴线片段视为有限平端圆柱；A-A 接触中轴线距离只作安全排除，侧面内部最近点可解析接受，端部候选使用 GJK 精算；
- A-B 接触采用“球心到有限平端实心圆柱的精确距离不超过 \(r_B+\delta\)”的判据，避免把靠近圆柱端角但实际未接触的球误判为连接；
- B-B 接触使用球心距离判据
  \(\|\boldsymbol c_i-\boldsymbol c_j\|\le2r_B+\delta\)；
- 越界球的周期片段用完整球镜像表示，并补齐两轴、三轴同时跨壁时的组合镜像；完整球镜像是实际球冠片段的超集近似；
- 球与电极接触按球心到有限正方形电极面的距离计算，不把电极视为无限平面。

因此，二维全域搜索的数值仍属于“当前几何修正版”结果。纯 A 退化情形将在下一节直接接入问题 3 的严格实体夹逼；对搜索得到的低成本含 B 临界候选，则另外使用统一实体内外界核正式复核。这样既保留快速搜索效率，又避免用近似球镜像认证最终方案。

### 2.2 与问题 3 的统一判定层

问题 4 若得到 \(N_B=0\)，模型便退化为问题 3 的纯 A 情形，此时不能继续使用上述近似几何单独给出最终答案，而应直接复用问题 3 的完整实体内接/外切多棱柱模型。为此，在原二维搜索之外增加纯 A 严格复核层：

- 外切多棱柱包含真实圆柱，给出导通概率上界；若其 Wilson 上限仍小于 0.90，可判定该数量可靠不足；
- 内接多棱柱包含于真实圆柱，给出导通概率下界；若其 Wilson 下限不小于 0.90，可判定该数量可靠可行；
- 其余数量均保留为待定，不用单一近似值强行判定。

因此，原 Q4 程序继续负责搜索“介质 B 是否可能降低成本”，最终纯 A 数量则由 Q3 严格实体模型决定。对于可能改变答案的低成本含 B 候选，另外使用 A 圆柱与 B 球体的统一内接/外切多面体实体核复核，不再使用完整球镜像近似作最终判定。

## 三、优化模型

### 3.1 决策变量、体积与成本

以两类完整介质数量为整数决策变量：

\[
N_A,N_B\in\mathbb Z_{\ge0}.
\tag{1}
\]

微构体体积为

\[
V_0=L^3=1000\,\mu\mathrm m^3.
\tag{2}
\]

介质 A、B 的单体体积为

\[
V_A=\pi(0.03)^2\times5
\approx0.01413717\,\mu\mathrm m^3,
\tag{3}
\]

\[
V_B=\frac43\pi(0.2)^3
\approx0.03351032\,\mu\mathrm m^3.
\tag{4}
\]

相应单件成本为

\[
c_A=1.05V_A\approx0.01484403\ \mathrm{元},
\qquad
c_B=0.05V_B\approx0.00167552\ \mathrm{元}.
\tag{5}
\]

总成本和体积分数为

\[
C(N_A,N_B)=c_AN_A+c_BN_B,
\tag{6}
\]

\[
\phi_A=\frac{N_AV_A}{V_0}\times100\%,
\qquad
\phi_B=\frac{N_BV_B}{V_0}\times100\%.
\tag{7}
\]

### 3.2 接触网络与导通状态

将周期处理后的 A、B 片段作为图节点，并增加左右电极节点 \(S,T\)。当片段间最短表面距离不超过 1.8 nm 时建立接触边；片段接触带电面时与相应电极节点连接。若 \(S,T\) 位于同一连通分量，则微构体导通。

第 \(s\) 个随机微构体在候选方案 \((N_A,N_B)\) 下的导通变量为

\[
Y_s(N_A,N_B)=
\begin{cases}
1,&S,T\text{连通},\\
0,&S,T\text{不连通}.
\end{cases}
\tag{8}
\]

蒙特卡洛概率估计为

\[
\widehat P(N_A,N_B)
=\frac1M\sum_{s=1}^{M}Y_s(N_A,N_B).
\tag{9}
\]

最终优化模型为

\[
\boxed{
\begin{aligned}
\min_{N_A,N_B\in\mathbb Z_{\ge0}}\quad
&c_AN_A+c_BN_B,\\
\mathrm{s.t.}\quad
&P(N_A,N_B)\ge0.90.
\end{aligned}}
\tag{10}
\]

### 3.3 共同随机数与样本级单调性

对每次试验预先生成固定顺序的介质序列

\[
A_1,A_2,\ldots,A_{N_A^{\max}},
\qquad
B_1,B_2,\ldots,B_{N_B^{\max}}.
\tag{11}
\]

候选 \((N_A,N_B)\) 使用两个序列的相应前缀。由于增加介质只增加节点和接触边，对每个固定随机样本有

\[
Y_s(N_A,N_B)\le Y_s(N_A+1,N_B),
\tag{12}
\]

\[
Y_s(N_A,N_B)\le Y_s(N_A,N_B+1).
\tag{13}
\]

该设计既保证样本级单调性，也减少相邻候选比较时的蒙特卡洛波动。正式运行的 100 次单调性构造检查中，违反次数为 0。

### 3.4 成本边界搜索

给定成本上限 \(C_0\) 和 A 的数量 \(N_A\)，该成本内可加入的最多 B 数量为

\[
N_B^{\max}(N_A;C_0)
=\left\lfloor\frac{C_0-c_AN_A}{c_B}\right\rfloor.
\tag{14}
\]

若在固定 \(N_A\) 下加入 \(N_B^{\max}\) 个 B 仍不能达到目标，则同成本内所有更少 B 的方案均可排除。由此把二维整数搜索压缩为成本边界上的一维非支配点扫描。

搜索流程为：

1. 分别计算纯 A、纯 B 基线，获得初始成本上界；
2. 在成本上进行二分可行性搜索；
3. 从二分结果向下搜索并以单个 B 的成本粒度细化；
4. 对最低点估计可行成本层进行完整非支配边界扫描；
5. 从该成本层开始逐层向上验证，直到发现 Wilson 可靠方案；
6. 对最终方案、低成本邻点、纯 A 锚点和纯 B 端点使用独立种子复算。

### 3.5 统计判定

候选点的 95% Wilson 区间为

\[
\frac{
\widehat P+\dfrac{z^2}{2M}
\pm z\sqrt{\dfrac{\widehat P(1-\widehat P)}{M}
+\dfrac{z^2}{4M^2}}
}{1+\dfrac{z^2}{M}},
\qquad z=1.959964.
\tag{15}
\]

记区间为 \([P_L,P_U]\)，判定规则为：

- \(P_U<0.90\)：不足；
- \(P_L\ge0.90\)：可靠可行；
- \(P_L<0.90\le P_U\)：区间跨线，当前样本不能定论。

搜索阶段使用 \(M_{\mathrm{search}}=100\) 定位；纯 A、纯 B 基线使用
\(M_{\mathrm{base}}=1500\)；正式独立验证使用 \(M_{\mathrm{final}}=4000\)。混合搜索结果优先取独立种子下成本最低的可靠候选；若其 \(N_B=0\)，再由 Q3 严格实体夹逼给出最终统一数量。

## 四、正式计算结果

### 4.1 基线与搜索定位

纯 A 基线得到：

\[
\widehat N_{A,90}=611,
\qquad
N_{A,\mathrm{safe}}=617.
\]

611 根 A 的成本为 9.069699 元；617 根 A 的成本为 9.158764 元。纯 B 在 8000 个球的搜索上限内没有形成满足要求的可行方案，正式基线样本中的最高点估计为 0。

以纯 A 点估计成本作为初始搜索上界，得到 \(N_A^{\max}=619\)、
\(N_B^{\max}=5415\)。低样本搜索定位结果为

\[
(N_A,N_B)=(598,62),
\]

成本 8.980609 元，\(M=100\) 时点估计恰为 0.9000。该点只用于定位，不能直接作为正式答案。

### 4.2 主种子验证带

从最低点估计可行成本层开始，共验证 3048 个非支配候选点：

| 主种子判定 | 候选点数 |
|---|---:|
| Wilson 上界低于 0.90，判定不足 | 3040 |
| Wilson 下界不低于 0.90，判定可靠 | 1 |
| Wilson 区间跨越 0.90 | 7 |

主种子验证得到的最低可靠方案为 \((617,0)\)。由于仍有 7 个候选的区间跨越 90% 线，这些点不能被判为可靠，也不能被严格判为不足。

### 4.3 独立种子复算

使用未参与搜索的新随机种子进行 \(M=4000\) 次复算，主要候选结果如下。

| \(N_A\) | \(N_B\) | 成本/元 | \(\widehat P\) | 95% Wilson 区间 | 判定 |
|---:|---:|---:|---:|---:|---|
| 609 | 5 | 9.048389 | 0.89550 | [0.88564, 0.90460] | 跨线 |
| 608 | 14 | 9.048625 | 0.89250 | [0.88252, 0.90173] | 跨线 |
| 610 | 4 | 9.061557 | 0.89775 | [0.88798, 0.90676] | 跨线 |
| 611 | 0 | 9.069699 | 0.89950 | [0.88980, 0.90844] | 跨线 |
| 610 | 12 | 9.074962 | 0.89775 | [0.88798, 0.90676] | 跨线 |
| 616 | 0 | 9.143920 | 0.90900 | [0.89969, 0.91752] | 跨线 |
| **617** | **0** | **9.158764** | **0.91075** | **[0.90152, 0.91920]** | **可靠** |
| 617 | 1 | 9.160439 | 0.91075 | [0.90152, 0.91920] | 可靠但成本更高 |
| 618 | 0 | 9.173608 | 0.91225 | [0.90308, 0.92063] | 可靠但成本更高 |

旧版结果 \((611,0)\) 在本次修正后独立复算中只有
\(\widehat P=0.8995\)，且 Wilson 下界低于 0.90，因此不能继续作为 95% 置信约束下的可靠答案。

### 4.4 当前 Q4 近似模型下的可靠方案

独立种子下成本最低的 Wilson 可靠方案为

\[
\boxed{N_A^*=617,\qquad N_B^*=0.}
\tag{16}
\]

对应体积分数为

\[
\phi_A^*=0.872263\%,
\qquad
\phi_B^*=0,
\tag{17}
\]

总成本为

\[
\boxed{C^*=9.158764\ \mathrm{元}.}
\tag{18}
\]

该方案在 \(M=4000\) 次独立试验中导通 3643 次，得到

\[
\widehat P=0.91075,
\qquad
95\%\ \mathrm{Wilson\ CI}
=[0.901516,0.919196].
\tag{19}
\]

由于区间下界 0.901516 大于 0.90，故该方案在本文置信约束下判为可靠可行。

### 4.5 纯 A 退化情形的统一复核

【数据来源：组长提供的问题 3 正式运行结果】问题 3 使用
\(M=10000\)、\(K=32\) 的实体内接/外切首次导通夹逼，得到经验临界数量区间

\[
N_{90}\in[613,616],
\]

并得到内接模型下满足 Wilson 95% 下限约束的保守安全数量

\[
N_{A,\mathrm{safe}}=619.
\]

本次新增的 Q4 统一复核程序又使用与 Q3 旧基准相同的
\(M=100,K=32,\text{seed}=20260808\) 做代码回归，准确复现了旧基准的经验临界
\([605,605]\) 和内接安全数量 632，且上下界违例为 0。该次 \(M=100\) 运行只用于验证程序一致性，不作为正式数值依据。

由于 Q4 搜索最终选择 \(N_B=0\)，正式推荐按 Q3 严格实体结果修正为

\[
\boxed{N_A=619,\qquad N_B=0.}
\tag{20}
\]

对应体积分数和成本为

\[
\phi_A=0.875091\%\approx0.88\%,
\qquad \phi_B=0,
\tag{21}
\]

\[
\boxed{C=619c_A=9.188452\ \mathrm{元}.}
\tag{22}
\]

这里的 617 是旧 Q4 近似几何在 \(M=4000\) 下得到的最低 Wilson 可靠点；619 是统一采用 Q3 严格实体夹逼后的保守推荐。两者相差 2 根 A，成本仅增加约 0.02969 元，但 619 的几何与问题 2、3 口径一致，故作为最终论文结果更稳妥。

### 4.6 含 B 临界候选的严格实体复核

【数据来源：本次严格混合实体程序正式运行】在原 Q4 搜索定位的候选集合上，采用共同随机数进行 \(M=4000\) 次复核。A 使用32边内接/外切多棱柱，B 使用三级细分二十面体的内接/外切多面体；B 的单侧径向夹逼误差上界约为 0.9098 nm。两类实体均先进行周期平移，再与基本盒严格裁剪，A-A、A-B、B-B 接触统一采用 AABB 预筛与凸体 GJK 距离。

主要候选结果如下：

| \(N_A\) | \(N_B\) | 成本/元 | 内界概率 | 内界Wilson下限 | 外界概率 | 外界Wilson上限 | 严格判定 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 598 | 62 | 8.980609 | 0.88050 | 0.87008 | 0.88700 | 0.89644 | 可靠不足 |
| 609 | 5 | 9.048389 | 0.90100 | 0.89136 | 0.90575 | 0.91442 | 待定 |
| 608 | 14 | 9.048625 | 0.90075 | 0.89110 | 0.90525 | 0.91394 | 待定 |
| 610 | 4 | 9.061557 | 0.90350 | 0.89396 | 0.90825 | 0.91681 | 待定 |
| 611 | 0 | 9.069699 | 0.90525 | 0.89578 | 0.90975 | 0.91824 | 待定 |
| 610 | 12 | 9.074962 | 0.90525 | 0.89578 | 0.90975 | 0.91824 | 待定 |
| 616 | 0 | 9.143920 | 0.91200 | 0.90282 | 0.91650 | 0.92468 | 可靠可行 |
| 617 | 0 | 9.158764 | 0.91375 | 0.90465 | 0.91800 | 0.92611 | 可靠可行 |
| 617 | 1 | 9.160439 | 0.91400 | 0.90491 | 0.91825 | 0.92634 | 可靠可行 |
| 618 | 0 | 9.173608 | 0.91600 | 0.90700 | 0.92025 | 0.92825 | 可靠可行 |
| 619 | 0 | 9.188452 | 0.91800 | 0.90909 | 0.92225 | 0.93015 | 可靠可行 |

全部候选、全部试验均满足“内界导通不强于外界导通”，上下界违例为 0。原小样本搜索点 \((598,62)\) 即使在外切实体上，Wilson上限也只有0.89644，因此可以严格排除。其余5个成本低于616根A的候选仍处于待定状态，尚不能用于宣称全局最优。

需要说明：本批 \(M=4000\) 共同样本中616根A已达到内界可靠标准，但Q3的正式结果使用 \(M=10000\)，其更大样本保守安全数量为619。为避免用较小样本覆盖更稳定的Q3结论，最终仍统一推荐619根；M=4000混合复核主要用于验证B候选和实体几何，而不重新定义Q3临界值。

## 五、结果分析

### 5.1 为什么最终没有选择介质 B

虽然 B 的单件成本只有 A 的约 11.3%，但 B 为半径 0.2 μm 的球体，缺乏 A 的长径比优势。在约 9 元成本范围内，纯 B 不能形成跨越 10 μm 微构体的稳定长程网络；加入少量 B 也未使低成本候选在独立复算中达到 Wilson 可靠标准。

例如小样本搜索得到的 \((598,62)\) 在 \(M=100\) 时恰好达到点估计 0.90，但这是从大量候选中筛选出的边界点，存在明显选择偏差，不能用 100 次结果直接认证。大样本验证最终把可靠方案推回纯 A 端点。这说明在当前尺寸、价格和随机分布下，B 的低价格不足以抵消其较弱的长程连通效率。

### 5.2 点估计最优与可靠方案的区别

点估计候选 \((598,62)\) 成本约 8.9806 元，比可靠方案低约 0.1782 元，但其依据只有 100 次搜索样本。可靠方案 \((617,0)\) 使用独立种子 4000 次试验，Wilson 下界达到 0.90。两者分别回答：

- 点估计搜索：哪个低成本点在当前小样本中看起来达到 90%；
- 置信约束：哪个方案能够在 95% 统计置信口径下可靠达到 90%。

题目要求导通概率“不低于 90%”，因此搜索阶段采用置信约束筛选；当最优配比退化为纯 A 后，再以 Q3 严格实体夹逼得到的 619 根作为统一保守推荐。

### 5.3 最优性表述边界

验证带中仍有 7 个候选点的 Wilson 区间跨越 0.90。这些候选没有被证明可靠，也没有被证明真实概率低于 0.90。因此，严格表述应为：

> \((617,0)\) 是原 Q4 近似几何已评估区域内成本最低的 Wilson 可靠方案；统一实体口径下的保守推荐为 \((619,0)\)，但含 B 的全局最优性仍未被严格证明。

在新增严格混合实体复核中，原 \((598,62)\) 已转为可靠不足，其余更低成本待定点缩减为5个。该结果加强了排除证据，但尚未消除全部统计待定点。

若必须证明所有更低成本点均不可行，需要继续增加跨线候选的样本量，或事先规定允许的统计无差异带。例如要求低成本候选的 Wilson 上界低于 0.90 才判不足，在真实概率十分接近 0.90 时可能需要远多于 4000 次试验。

### 5.4 跨壁比例自检

固定种子的 2000 根几何自检得到：

- A 跨壁比例约 0.6085，与轴线理论基准约 0.6008 在小样本误差范围内一致；
- B 跨壁比例约 0.1120，与理论值
  \(1-(1-2r_B/L)^3\approx0.1153\) 接近。

跨壁比例只用于检查周期边界实现是否异常，不作为优化模型的额外输入。

## 六、模型检验与计算

### 6.1 自动测试

Q4 快速搜索、纯A统一复核及严格混合实体核共 62 项自动测试全部通过，主要覆盖：

1. 球体单轴、双轴、三轴跨壁时的组合周期镜像；
2. 有限正方形电极面与球片段的接触距离；
3. A-A 平端圆柱端部误接触回归；
4. A-B 圆柱端角误接触回归；
5. B-B 阈值等号及阈值外判定；
6. 空间网格球对查询与小样本暴力枚举逐对一致；
7. 共同随机数下增加 A 或 B 的样本级单调性；
8. 成本层搜索、Wilson 判定、分级追加和纯 A 锚点兜底。
9. 实体内外界经验曲线单调性、严格三分类和样本级夹逼关系。
10. 球体内外接多面体误差随细分收敛、跨壁实体裁剪和纯B链路。
11. Q4在 \(N_B=0\) 时与Q3逐样本一致，混合前缀保持单调。

### 6.2 球对搜索内存优化

原程序在无 SciPy 环境下构造 \(n\times n\) 全矩阵。8000 个原始球经周期镜像后约有 9000 个球片段，单个 \(8974\times8974\) 浮点临时数组即需约 614 MiB，多进程运行会导致内存不足。

修正版采用三维空间网格：网格边长取 B-B 接触阈值
\(2r_B+\delta=401.8\,\mathrm{nm}\)，每个球只检查自身网格及相邻 26 个网格。若两球距离不超过阈值，其网格编号在任一坐标上至多相差 1，因此该方法不会漏掉接触对。约 9000 个镜像的实测峰值内存由数百 MiB 降至约 3.4 MiB，完整纯 B 单样本峰值约 4.8 MiB。

### 6.3 正式计算设置

| 参数 | 数值 |
|---|---:|
| 搜索样本数 \(M_{\mathrm{search}}\) | 100 |
| 正式验证样本数 \(M_{\mathrm{final}}\) | 4000 |
| 纯介质基线样本数 \(M_{\mathrm{base}}\) | 1500 |
| 锚点最大样本数 \(M_{\mathrm{anchor}}\) | 8000 |
| 并行进程数 | 16 |
| 主随机种子 | 42 |
| 独立验证种子偏移 | 12345 |
| 验证带候选点数 | 3048 |
| 全部记录点评估数 | 12644 |
| 总耗时 | 3391.7 s，约 56.5 min |

严格混合候选复核另采用以下设置：

| 参数 | 数值 |
|---|---:|
| 候选组合数 | 11 |
| 共同随机样本数 | 4000 |
| A横截面边数 | 32 |
| B二十面体细分级数 | 3 |
| B径向误差上界 | 0.9098 nm |
| 并行进程数 | 16 |
| 基础随机种子 | 20260808 |
| 上下界违例数 | 0 |
| 累计计算时间 | 约 3077 s，约 51.3 min |

## 七、与旧版的区别

| 项目 | 旧版 | 几何修正版 |
|---|---|---|
| A-A 接触 | 轴线胶囊距离可能误接平端 | 内部侧面安全接受，端部调用 GJK |
| A-B 接触 | 球心到轴线阈值可能误接端角 | 球心到有限实心圆柱的精确距离 |
| B 周期镜像 | 多方向跨壁可能漏组合镜像 | 补齐双轴、三轴组合镜像 |
| B 与电极 | 可能按无限平面判断 | 按有限正方形电极面判断 |
| B-B 候选 | 无 SciPy 时全矩阵，内存爆炸 | 三维空间网格，近似线性内存 |
| 正式可靠方案 | \((611,0)\)，旧几何下独立结果 | \((617,0)\)，新几何下独立可靠结果 |
| 几何局限 | 未集中说明 | 快速全域搜索保留近似；正式临界候选使用实体夹逼 |
| 纯 A 最终口径 | Q4 单独判定 | 退化时直接复用 Q3 实体夹逼，推荐 \((619,0)\) |
| 含 B 临界点 | 完整球镜像近似 | A、B统一内外接多面体实体夹逼 |
| 低样本点 \((598,62)\) | 点估计达到0.90 | 外界Wilson上限0.89644，严格判定不足 |

## 八、待验证与适用边界

1. **低成本待定候选追加样本**：原快速验证的7个跨线候选经严格实体复核后已有1个可靠不足，当前仍有5个成本低于616根A的候选待定。
2. **扩大严格混合样本**：当前严格混合复核为 \(M=4000\)，仍有5个更低成本候选待定；可利用检查点继续扩展至 \(M=10000\)。
3. **提高几何分辨率敏感性**：当前A为32边、B为三级细分，夹逼关系严格成立；若需进一步缩小几何概率间隙，可增加A边数或B细分级数。
4. **独立主种子复算**：本次已经用搜索外独立种子验证候选，但若要求跨运行稳健性，还可更换另一主种子重复整个搜索。
5. **接触阈值敏感性**：应对 \(\delta=1.5\sim2.0\,\mathrm{nm}\) 进行逐点扫描。
6. **随机分布敏感性**：若实际工艺存在方向偏好或空间聚集，最优配比可能发生变化。

## 九、结论

在“介质中心均匀、A 方向各向同性、允许重叠、周期边界后同源片段不自动连接、表面距离不超过 1.8 nm 视为接触”的统一口径下，Q4 二维成本搜索表明介质 B 暂未显示出成本优势，最优方向退化为纯 A。原 Q4 近似几何在独立种子 \(M=4000\) 次验证中得到最低 Wilson 可靠点 \((617,0)\)；进一步复用 Q3 的 \(M=10000\)、\(K=32\) 严格实体夹逼后，最终保守推荐修正为：

\[
\boxed{
(N_A,N_B)=(619,0),
\quad
C=9.188452\ \mathrm{元},
\quad
\phi_A=0.875091\%\approx0.88\%,
\quad
\phi_B=0.
}
\]

其中 619 根来自问题 3 内接实体模型的 Wilson 95% 安全数量，而不是由 617 根的近似概率外推得到。按体积分数保留两位可写为 A 占 0.88%、B 占 0.00%。介质 B 虽单位体积价格较低，但在本题尺寸与随机分布下未显示出足以降低可靠成本的长程连通效率。

新增严格混合实体复核进一步表明：原搜索点 \((598,62)\) 已可严格排除，619根纯A在内接实体模型下可靠可行，且4000次试验没有出现上下界违例。不过仍有5个成本更低的候选处于统计待定状态。因此，严谨结论是“当前优化证据支持不采用B，统一保守推荐为 \((619,0)\)”，而不是已经严格证明了所有混合整数方案的全局最优性。
