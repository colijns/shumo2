两件事：

## 1. 同步 skill 到 ZCode 项目级目录

- 复制 `.claude/skills/competition-record/`（SKILL.md + references/ 下 6 个模板文件）→ `.zcode/skills/competition-record/`
- 复制 `.claude/skills/math-modeling-report/`（SKILL.md）→ `.zcode/skills/math-modeling-report/`
- 两个 SKILL.md 均无 Claude 专有引用，原样复制即可，不做改写

## 2. 创建 `D:\code_warehouse\Projects\shumo3\AGENTS.md`

内容为简明中文说明文件（从 CLAUDE.md 与仓库勘察提炼，自包含）：

1. **项目概述** — 数学建模比赛仓库：`templates/` 代码模板库 + 按题号（Q1/Q2/Q3）划分的解题工作区
2. **环境与命令** — conda env `math`（非交互 shell 用全路径 python）；GBK 编码坑（不要用 `conda run`）；pytest 命令；requirements.txt 不全需按需补装
3. **目录结构** — templates 八大类 + common、docs/（题目.md、appendix/）、attachment/、题号目录、`.claude/`（hooks 留存机制）
4. **模板使用铁律** — templates/ 只复制不修改；单文件自包含；返回 dict；固定 seed；savefig + Agg 后端；load_table/export_result IO
5. **项目级 skills** — `competition-record` 与 `math-modeling-report` 在 `.zcode/skills/`（从 `.claude/skills/` 镜像，更新时两处同步改）
6. **AI 使用留存（强制）** — hook 自动归档 prompt/transcript 至 docs/appendix/，绘图等按 competition-record skill 步骤执行，纪律见 docs/appendix/README.md
7. **当前赛题** — A 题多无人机协同巡检路径优化，附件与 result1~3.xlsx 输出格式说明
8. **工作流** — 判题型 → 查 templates/MODULES.md → 跑 demo → 复制到题号目录 → 换赛题数据 → 出图 → 固定 seed → 跑测试
9. 文末注明与 CLAUDE.md 内容对应，修改约定时两份同步更新

完成后汇报写入的章节与文件路径。