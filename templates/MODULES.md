# 模块速查表（AI 适配用）

> 比赛时由 Claude 适配模块用。每行一个模块,扫此表选模块,再读对应 `.py` 的 demo 改数据。

| 问题信号 | 模块路径 | 核心函数 | 输入形状 | 输出 dict 键 | 备注 |
|---|---|---|---|---|---|
| 多指标方案排序/综合评价 | 01_evaluation/topsis.py | entropy_topsis(X, types) | (n方案×m指标) ndarray + types | scores, ranking, weights_used | types: benefit/cost;熵权自动定权 |
| 主观赋权/一致性检验 | 01_evaluation/ahp.py | ahp(A) | (n×n) ndarray 方阵 | weights, lambda_max, CI, CR, consistent | n 阶正互反判断矩阵;CR<0.1 通过检验 |
| 客观赋权(熵权) | 01_evaluation/entropy_weight.py | entropy_weight(X, types) | (n×m) ndarray + types | (ndarray) 权重向量 | types: benefit/cost;返回 ndarray 非 dict |
| 模糊综合评价 | 01_evaluation/fuzzy_evaluation.py | fuzzy_evaluate(R, w, operator) | (m×n) ndarray R + (m,) w + str | (ndarray) 综合隶属度 | operator: minmax/multiply;返回 ndarray 非 dict |
| 客观赋权(CRITIC) | 01_evaluation/critic_weight.py | critic_weight(X) | (n×m) ndarray | (ndarray) 权重向量 | 综合标准差与相关系数;返回 ndarray 非 dict |
| 灰色关联分析 | 01_evaluation/grey_relation.py | grey_relational(X, ref_index, rho) | (m×n) ndarray + int + float | (ndarray) 关联度 | ref_index 参考行号;rho 分辨系数默认 0.5;返回 ndarray |
| PCA 降维综合评价 | 01_evaluation/pca_evaluation.py | pca_evaluate(X, threshold) | (n×m) ndarray + float | scores, ranking, n_components, explained_ratio, loadings | threshold 累计方差贡献率阈值默认 0.85 |
| 秩和比综合评价 | 01_evaluation/rsr.py | rsr(X, w, types) | (n×m) ndarray + (m,) w/None + types | rsr, ranking, probit | types: benefit/cost;w=None 为等权 |
| 灰色预测/小样本趋势 | 02_prediction/grey_model.py | gm11(x0, n_predict) | (n,) ndarray + int | a, b, fitted, predict, relative_error, C, P, grade | n>=4;后验差检验精度等级 |
| 指数平滑/趋势季节预测 | 02_prediction/exponential_smoothing.py | ets_forecast(series, trend, seasonal, seasonal_periods, steps) | (n,) ndarray + str + str/None + int/None + int | (ndarray) 预测值 | 返回 ndarray 非 dict;基于 statsmodels |
| 多元线性回归/多项式/曲线拟合 | 02_prediction/regression.py | linear_regression / poly_fit / curve_fit_custom | (n×m) ndarray+(n,) / (n,)+(n,)+int / (n,)+(n,)+callable | coef,intercept,r2,model / (ndarray)系数 / params,pcov,y_fit | 3 函数;可选 statsmodels OLS |
| 马尔可夫链/状态转移 | 02_prediction/markov_chain.py | MarkovChain(P).n_step/predict_distribution/steady_state | (n×n) ndarray | (ndarray) 转移矩阵/分布/稳态 | 类接口;3 方法 |
| BP 神经网络预测 | 02_prediction/bp_neural.py | bp_forecast(series, lag, test_ratio, steps, seed) | (n,) ndarray + int + float + int + int/None | y_pred_test, y_true_test, future, metrics, model | seed 控制可复现;MLPRegressor |
| ARIMA 时间序列预测/自动定阶 | 02_prediction/arima_model.py | select_order_aic(series, p_max, d_max, q_max) / arima_forecast(series, order, steps) | (n,) ndarray + int+int+int / (n,) ndarray + (3,) tuple + int | select_order_aic 返回 (best_order, best_aic) tuple; arima_forecast 返回 {forecast, conf_int, model_summary_ic} | 网格搜索 AIC 自动定阶;基于 statsmodels;含置信区间;有绘图 |
| 生产计划/资源分配/运输调配/配料 | 03_optimization/linear_programming.py | solve_lp(c, A_ub, b_ub, A_eq, b_eq, bounds) | c=(n,); A_ub=(m,n); b_ub=(m,); A_eq=(k,n); b_eq=(k,); bounds=[(lo,hi)]*n | x, fun, success, message | 基于 scipy HiGHS;无画图 |
| 0-1 背包/选址/指派/生产计划取整 | 03_optimization/integer_programming.py | solve_knapsack(values, weights, capacity) / solve_integer_production(profits, resource_use, resource_avail) | values=(n,); weights=(n,); capacity=scalar / profits=(n,); resource_use=(m,n); resource_avail=(m,) | {chosen, x, fun, status} / {x, fun, status} | 可选依赖 PuLP;无画图;测试用 importorskip |
| 最优设计/经济调度/曲线拟合型优化 | 03_optimization/nonlinear_programming.py | solve_nlp(func, x0, bounds, constraints) | func: R^n→R; x0=(n,); bounds=[(lo,hi)]*n; constraints=[dict,...] | x, fun, success, message, nit | 基于 scipy SLSQP;有可视化图（nlp_demo.png） |
| 多目标决策/方案优选/帕累托筛选后决策 | 03_optimization/multi_objective.py | weighted_sum(F, weights) / ideal_point(F, types) | F=(n_samples, n_obj); weights=(n_obj,); types=[str]*n_obj | {scores, ranking} / {scores, ranking, ideal, anti_ideal} | 无画图;内部自动极差归一化 |
| 遗传算法/连续函数全局最小化/组合优化初解 | 03_optimization/genetic_algorithm.py | ga_minimize(func, dim, bounds, pop_size, max_gen, pc, pm, seed) | func: R^n→R; dim: int; bounds: (dim, 2); 其余参数: scalar; seed: int/None | best_x, best_f, history | 实数编码;锦标赛选择;算术交叉;高斯变异;seed 可复现;有绘图 |
| 粒子群算法/连续函数全局最小化/参数整定 | 03_optimization/particle_swarm.py | pso_minimize(func, dim, bounds, swarm_size, max_iter, w, c1, c2, seed) | func: R^n→R; dim: int; bounds: (dim, 2); 其余参数: scalar; seed: int/None | best_x, best_f, history | 惯性权重+个体/社会学习因子;速度限幅;seed 可复现;有绘图 |
| 模拟退火/连续函数最小化/TSP 路径规划 | 03_optimization/simulated_annealing.py | sa_minimize(func, x0, bounds, T0, alpha, T_min, iter_per_T, seed) / sa_tsp(coords, T0, alpha, seed) | func: R^n→R; x0: (n,); bounds: (n,2); 其余参数: scalar; seed: int/None / coords: (n,2) | {best_x, best_f, history} / {route, distance, history} | 2 接口(连续+TSP);几何降温;Metropolis 准则;seed 可复现;有绘图 |
| 描述性统计/EDA 数据探索 | 04_statistics/descriptive.py | describe_all(df) | (n×m) DataFrame | (pandas.DataFrame) 9统计量行×m列 | 均值/中位数/标准差/方差/偏度/峰度/极值;含直方图 |
| 变量间相关分析/指标筛选 | 04_statistics/correlation.py | correlation_analysis(df, method) | (n×m) DataFrame + str | coef, p_value | method: pearson/spearman/kendall;含热力图 |
| 数据补全/曲面重构/经验公式拟合 | 04_statistics/interpolation_fitting.py | demo_interp1d / demo_griddata / demo_polyfit | (n,)数组+插值/ (n,)散乱点+网格/ (n,)+(n,)+int | (tuple) 插值函数 / (tuple) 网格坐标 / (ndarray) 系数 | 3 案例函数;interp1d/griddata/polyfit |
| 蒙特卡洛模拟/π估算/定积分/报童模型库存仿真 | 04_statistics/monte_carlo.py | estimate_pi(n, seed) / monte_carlo_integral(func, a, b, n, seed) / simulate_inventory(days, demand_lambda, stock, price, cost, salvage, seed) | n: int+seed / func+(a,b)+n+seed / days+demand_lambda+stock+price+cost+salvage+seed | estimate_pi 返回 float; monte_carlo_integral 返回 float; simulate_inventory 返回 {daily_profit, mean_profit, std_profit, sold_out_rate, params} | 3 函数;seed 可复现;含绘图 |
| 假设检验/正态性检验/t检验/方差分析/卡方检验 | 04_statistics/hypothesis_tests.py | normality_test(x) / one_sample_t(x, mu) / two_sample_t(x, y, equal_var) / paired_t(x, y) / anova_oneway(*groups) / chi2_independence(contingency) | (n,) ndarray + 参数 | 所有函数返回 (stat, p) tuple | 6 函数;默认 Welch t(equal_var=False);α=0.05;中文结论打印 |
| K-means 聚类/肘部法+轮廓系数自动选 k | 05_clustering_classification/kmeans.py | kmeans_auto(X, k_range, seed) | (n_samples, n_features) ndarray + range + int/None | best_k, labels, centers, sse_list, silhouette_list, k_list | seed 可复现;含肘部图+轮廓图 |
| 层次聚类/谱系分类/树状图 | 05_clustering_classification/hierarchical.py | hierarchical_cluster(X, n_clusters, method) / plot_dendrogram(X, method, save_path) | (n_samples, n_features) ndarray + int + str | labels (ndarray) / Z (plot_dendrogram 返回链接矩阵) | method: ward/average/complete/single;含树状图 |
| 决策树与随机森林分类/特征重要性 | 05_clustering_classification/decision_tree_rf.py | train_tree_rf(X, y, test_size, seed) | (n_samples, n_features) X + (n_samples,) y + float + int/None | tree, rf, tree_acc, rf_acc, tree_cm, rf_cm, importances | seed 可复现;同时训练决策树+随机森林;含混淆矩阵+重要性图 |
| 支持向量机分类/网格搜索调参 | 05_clustering_classification/svm_classifier.py | svm_grid_search(X, y, C_grid, gamma_grid, test_size, cv, random_state) | (n_samples, n_features) X + (n_samples,) y + tuple+tuple+float+int+int | best_params, best_score, test_acc, cm, model, cv_results | RBF 核;标准化管线;GridSearchCV 交叉验证;含热力图 |
| 图论-最短路径 | 06_graph_network/shortest_path.py | single_source_shortest(G, source) / all_pairs_shortest(G) | G: nx.Graph, source: node | {distances, paths} / DataFrame | Dijkstra;all_pairs_shortest 返回 DataFrame;有绘图 |
| 图论-最小生成树 | 06_graph_network/mst.py | solve_mst(G, algorithm) / build_cost_graph(cities, edges) | G: nx.Graph, algorithm: str | {edges, total_weight, mst_graph} | 支持 kruskal/prim/boruvka;有绘图 |
| 图论-最大流 | 06_graph_network/max_flow.py | solve_max_flow(G, source, sink) / build_flow_network(edges) | G: nx.DiGraph, source/sink: node | {max_flow_value, edge_flows, min_cut} | Ford-Fulkerson;附最小割;有绘图 |
| 常微分方程数值求解/Logistic/SIR/Lotka-Volterra | 07_simulation_ode/ode_solver.py | solve_logistic(r, K, x0, t_span, n_points) / solve_sir(beta, gamma, S0, I0, R0, t_span, n_points) / solve_lotka_volterra(alpha, beta, delta, gamma, x0, y0, t_span, n_points) | 各参数: float; t_span: (float,float); n_points: int | {t, x, r, K} / {t, S, I, R, R0_basic} / {t, prey, predator, equilibrium} | 基于 scipy solve_ivp;RK45;3 经典案例;有绘图 |
| 差分方程/等额本息还款/离散 Logistic 混沌 | 07_simulation_ode/difference_equation.py | equal_installment(principal, annual_rate, n_months) / discrete_logistic(r, x0, n) | principal+annual_rate+n_months: float+float+int / r+x0+n: float+float+int | {monthly_payment, total_payment, total_interest, schedule} / ndarray | schedule 为 pandas DataFrame;含还款计划表+分岔图 |
| M/M/1 排队论/Little 公式解析解+离散事件仿真 | 07_simulation_ode/queueing.py | mm1_metrics(lam, mu) / mm1_simulate(lam, mu, n_customers, seed) | lam, mu: float / lam, mu: float, n_customers: int, seed: int/None | {rho, L, Lq, W, Wq} / {rho, L, Lq, W, Wq, n_customers} | mm1_simulate 含热身期(舍弃前 10%);seed 可复现;无绘图 |
| 基础图表绘制模板/折线/柱状/散点/饼图 | 08_plot_templates/basic_plots.py | plot_line / plot_grouped_bar / plot_stacked_bar / plot_scatter_regression / plot_pie | (x, series_dict) / (categories, series_dict) / ... | Figure (所有函数) + demo PNG 5 张 | Agg 后端;savefig 代替 plt.show |
| 统计图表模板/热力/箱线/小提琴/雷达图 | 08_plot_templates/statistical_plots.py | plot_heatmap / plot_boxplot / plot_violin / plot_radar | (corr: DataFrame) / (df: DataFrame) / (df: DataFrame) / (labels, series_dict) | Figure (所有函数) + demo PNG 4 张 | Agg 后端;依赖 seaborn |
| 3D 曲面/等高线/中国地图 | 08_plot_templates/maps_3d.py | plot_surface / plot_contour / plot_china_map_pyecharts | (X,Y,Z: meshgrid) / (X,Y,Z) / (province_data: list) | Figure (mpl 函数) / bool (pyecharts) + demo PNG 2 张 | pyecharts 为可选依赖;importorskip 保护 |
| 数据预处理工具箱/归一化/标准化/正向化/缺失值填充 | common/data_utils.py | min_max_normalize(X, axis) / z_score_standardize(X, axis) / forward_transform(X, types, bounds, best) / fill_missing(df, strategy, columns) | X: (n×m) ndarray; axis: int; types: [str]; bounds: list; best: list; df: DataFrame | ndarray (各函数) | 4 函数;防除零;正向化支持效益/成本/区间/中间型 |
| 预测精度评价/RMSE/MAE/MAPE/R² | common/metrics.py | rmse(y_true, y_pred) / mae(y_true, y_pred) / mape(y_true, y_pred) / r2_score(y_true, y_pred) | y_true: (n,) ndarray; y_pred: (n,) ndarray | float (各函数) | 4 函数;自动长度校验;MAPE 自动剔除零值样本 |
| 数据 IO/按扩展名自动读写 csv/xlsx | common/io_utils.py | load_table(path, sheet_name) / export_result(obj, path) | path: str; sheet_name: int/str; obj: DataFrame/dict | DataFrame / str (绝对路径) | 支持 .csv/.xlsx/.xls;自动识别扩展名 |
| 绘图样式统一设置/中文rcParams/savefig | common/plot_style.py | set_chinese_style() / save_fig(fig, path, dpi) | 无 / (Figure, str, int) | None / str (保存路径) | 条件 Agg 切换避免 rcParams 重置;含 demo PNG |
